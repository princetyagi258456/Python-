
Write a Python function to multiply all the numbers in a list.
Sample List : [1, 2, 3, -4]
Expected Output : -24


def multiply(numbers):
    total=numbers[0]
    for mul in numbers:
        total*=mul
    return total
        
 multiply([1,2,3,4])
 24
